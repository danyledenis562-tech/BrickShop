<x-app-layout>
    <div class="mx-auto max-w-5xl px-4 py-10">
        <h1 class="text-4xl font-extrabold">{{ __('messages.policy') }}</h1>
        <p class="mt-4 text-[color:var(--muted)]">{{ __('messages.policy_text_1') }}</p>
        <p class="mt-3 text-[color:var(--muted)]">{{ __('messages.policy_text_2') }}</p>
        <p class="mt-3 text-[color:var(--muted)]">{{ __('messages.policy_text_3') }}</p>
    </div>
</x-app-layout>
